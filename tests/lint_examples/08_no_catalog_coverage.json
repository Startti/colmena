{
  "nodes": {
    "agent": {
      "type": "llm_call",
      "config": {
        "provider": "openai",
        "api_key": "${OPENAI_API_KEY}",
        "model": "gpt-4o",
        "prompt": "use the tool",
        "tool_configurations": {
          "flux": {
            "node_type": "quantum_flux_capacitor",
            "node_schema": {
              "charge": {
                "type": "number",
                "required": true,
                "description": "how much"
              }
            }
          }
        }
      }
    }
  },
  "edges": [],
  "_comment": "NO_CATALOG_COVERAGE (info) — a tool targets a node type the catalog does not document, so its configuration was not checked. The node-level arm no longer produces this: absence from the catalog proves the engine cannot run the type, so it is an error there."
}
