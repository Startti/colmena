{
  "comment": "Test: LLM agent that builds ADP canvas graphs using on-demand skills (one reference per node type) instead of dumping all node configs into the system prompt.",
  "metadata": {
    "category": "external",
    "features": [
      "socketio_tool",
      "llm_call",
      "node_schema",
      "skills"
    ],
    "requires_env": [
      "GEMINI_API_KEY",
      "ADP_API_URL",
      "ADP_SESSION_TOKEN",
      "ADP_ENVIRONMENT_ID",
      "ADP_WORKSPACE_ID"
    ]
  },
  "nodes": {
    "trigger": {
      "type": "input",
      "config": {
        "prompt": "Quiero un agente que reciba el nombre de un futbolista del usuario y arme un reporte detallado sobre él. Que pueda buscar información en internet."
      }
    },
    "agent": {
      "type": "llm_call",
      "config": {
        "provider": "gemini",
        "model": "gemini-2.5-flash",
        "api_key": "${GEMINI_API_KEY}",
        "connection_url": "${DATABASE_URL}",
        "system_message": "You are a canvas automation agent for the ADP platform. You manage nodes and edges on a visual workflow canvas.\n\nWORKFLOW:\n1. Always call load_canvas first to see existing nodes, edges, and groups.\n2. Before creating any node, call load_skill('adp-node-catalog', '<type>') to get its data.config schema, connection rules, runtime behavior, and examples. Do NOT fabricate node configs from memory.\n3. Use create_canvas_node with the type and category from the reference. Position new nodes near related nodes; reuse the existing layout as a guide.\n4. Connect nodes with create_edge. Default to a vertical layout (sourceHandle='bottom', targetHandle='top'). For condition branches use sourceHandle='true' or 'false'. For AI→tool edges: source is the AI node, target is the tool (tools have NO output handles).\n5. For AI nodes (anything with category 'ai'), call list_api_keys first and present the available keys. ASK the user before fabricating any secret key — never guess one.\n\nLAYOUT & SPACING (critical — nodes overlap if you don't follow this):\n- Each node visually occupies ~250px wide × 50px tall.\n- Vertical flow (trigger → ai → output): place each next node Δy=180-220 below the previous one, same x.\n- Tools attached to an AI node: place to the side (Δx=400 from the AI's x), STACKED vertically with Δy=120 between tools. Do NOT place all tools at the same y as the AI or they will overlap.\n- Before placing a new node, scan existing positions in load_canvas output. Pick coordinates that are at least 350px away horizontally OR 120px vertically from every existing node.\n\nThe full type catalog is the skill 'adp-node-catalog'. Load only the references for the node types you actually intend to create.",
        "temperature": 0.0,
        "stream": false,
        "skills": {
          "paths": ["/app/skills/adp-node-catalog"]
        },
        "tool_configurations": {
          "load_canvas": {
            "name": "load_canvas",
            "node_type": "socketio_request",
            "description": "Load the current canvas state for the environment. Returns all existing nodes, edges, and groups. Always call this first to understand the current layout before making changes.",
            "node_schema": {
              "url": {
                "type": "string",
                "fixed": "${ADP_API_URL}"
              },
              "namespace": {
                "type": "string",
                "fixed": "/canvas"
              },
              "event": {
                "type": "string",
                "fixed": "load_canvas_state"
              },
              "wait_event": {
                "type": "string",
                "fixed": "canvas_state_loaded"
              },
              "cookies": {
                "type": "string",
                "fixed": "__Secure-better-auth.session_token=${ADP_SESSION_TOKEN}"
              },
              "timeout_ms": {
                "type": "integer",
                "fixed": 15000
              },
              "payload": {
                "type": "object",
                "properties": {
                  "environmentId": {
                    "type": "string",
                    "fixed": "${ADP_ENVIRONMENT_ID}"
                  }
                }
              }
            }
          },
          "create_canvas_node": {
            "name": "create_canvas_node",
            "node_type": "socketio_request",
            "description": "Create a new node on the canvas. Returns the created node with its generated ID.\n\nPayload: { environmentId (auto), node: { type, category, position: { x, y }, data: { label, config? }, measured?: { width, height }, groupId?: string|null, sortIndex?: number } }.\n\nThe exact data.config schema, the type→category mapping, and the connection rules for each node type live in the skill 'adp-node-catalog'. Call load_skill('adp-node-catalog', '<type>') BEFORE invoking this tool — do not guess configs from memory.",
            "node_schema": {
              "url": {
                "type": "string",
                "fixed": "${ADP_API_URL}"
              },
              "namespace": {
                "type": "string",
                "fixed": "/canvas"
              },
              "event": {
                "type": "string",
                "fixed": "create_node"
              },
              "cookies": {
                "type": "string",
                "fixed": "__Secure-better-auth.session_token=${ADP_SESSION_TOKEN}"
              },
              "timeout_ms": {
                "type": "integer",
                "fixed": 15000
              },
              "payload": {
                "type": "object",
                "properties": {
                  "environmentId": {
                    "type": "string",
                    "fixed": "${ADP_ENVIRONMENT_ID}"
                  },
                  "node": {
                    "type": "object",
                    "required": true,
                    "description": "Node object: { type: string, category: string, position: { x: number, y: number }, data: { label: string, config?: object }, measured?: { width: number, height: number }, groupId?: string | null, sortIndex?: number }"
                  }
                }
              }
            }
          },
          "update_canvas_node": {
            "name": "update_canvas_node",
            "node_type": "socketio_request",
            "description": "Update an existing node on the canvas. Provide the node ID and an updates object with the fields to change. WARNING: The backend replaces the entire 'data' object instead of deep merging. If you need to update data.label or data.config, send the COMPLETE data object including all existing fields, not just the changed ones.",
            "node_schema": {
              "url": {
                "type": "string",
                "fixed": "${ADP_API_URL}"
              },
              "namespace": {
                "type": "string",
                "fixed": "/canvas"
              },
              "event": {
                "type": "string",
                "fixed": "update_node"
              },
              "cookies": {
                "type": "string",
                "fixed": "__Secure-better-auth.session_token=${ADP_SESSION_TOKEN}"
              },
              "timeout_ms": {
                "type": "integer",
                "fixed": 15000
              },
              "payload": {
                "type": "object",
                "properties": {
                  "environmentId": {
                    "type": "string",
                    "fixed": "${ADP_ENVIRONMENT_ID}"
                  },
                  "id": {
                    "type": "string",
                    "required": true,
                    "description": "The ID of the node to update"
                  },
                  "updates": {
                    "type": "object",
                    "required": true,
                    "description": "Object with the fields to update. For position: { position: { x, y } }. For data: send the COMPLETE data object { data: { label, config, ... } } because it replaces entirely."
                  }
                }
              }
            }
          },
          "delete_canvas_node": {
            "name": "delete_canvas_node",
            "node_type": "socketio_request",
            "description": "Delete a node from the canvas by its ID. Also removes any edges connected to this node and resolves groups automatically.",
            "node_schema": {
              "url": {
                "type": "string",
                "fixed": "${ADP_API_URL}"
              },
              "namespace": {
                "type": "string",
                "fixed": "/canvas"
              },
              "event": {
                "type": "string",
                "fixed": "delete_node"
              },
              "cookies": {
                "type": "string",
                "fixed": "__Secure-better-auth.session_token=${ADP_SESSION_TOKEN}"
              },
              "timeout_ms": {
                "type": "integer",
                "fixed": 15000
              },
              "payload": {
                "type": "object",
                "properties": {
                  "environmentId": {
                    "type": "string",
                    "fixed": "${ADP_ENVIRONMENT_ID}"
                  },
                  "nodeId": {
                    "type": "string",
                    "required": true,
                    "description": "The ID of the node to delete"
                  }
                }
              }
            }
          },
          "create_edge": {
            "name": "create_edge",
            "node_type": "socketio_request",
            "description": "Create an edge (connection) between two nodes. Provide an 'edge' object with the connection details. The fields type='default', animated=true, and environmentId are auto-filled into the edge.\n\nEDGE OBJECT FIELDS:\n- id: generate a random 24-char alphanumeric string (REQUIRED)\n- source: source node ID (REQUIRED)\n- target: target node ID (REQUIRED)\n- sourceHandle: output handle on source node (OPTIONAL)\n- targetHandle: input handle on target node (OPTIONAL)\n- type, animated, environmentId: AUTO-FILLED — do not include\n\nHANDLE BEHAVIOR BY NODE TYPE:\n- TRIGGER nodes (chatInput, webhook, schedule): output-only handles (bottom, right, left)\n- LLM/Agent nodes (llmCall, agent): initially all input; after first connection, remaining become output\n- Tool nodes (webSearch, apiCall, etc.): input-only (connect TO ai nodes)\n- Condition: inputs (top, left, right) + outputs named 'true' and 'false'\n- Output nodes (chatOutput, etc.): input-only (terminal)\n\nRECOMMENDED HANDLES:\n- trigger -> ai: sourceHandle='bottom', targetHandle='top'\n- ai -> output: sourceHandle='bottom', targetHandle='top'\n- ai -> tool: source MUST be the ai node, target MUST be the tool. Use sourceHandle='left'/'right' on the ai, targetHandle='right'/'left' on the tool. ⚠️ Tools have NO output handles — reversed edges (source=tool, target=ai) are silently accepted by the canvas API but the runtime will NOT wire the tool. ALWAYS verify direction.",
            "node_schema": {
              "url": {
                "type": "string",
                "fixed": "${ADP_API_URL}"
              },
              "namespace": {
                "type": "string",
                "fixed": "/canvas"
              },
              "event": {
                "type": "string",
                "fixed": "create_edge"
              },
              "cookies": {
                "type": "string",
                "fixed": "__Secure-better-auth.session_token=${ADP_SESSION_TOKEN}"
              },
              "timeout_ms": {
                "type": "integer",
                "fixed": 15000
              },
              "payload": {
                "type": "object",
                "properties": {
                  "environmentId": {
                    "type": "string",
                    "fixed": "${ADP_ENVIRONMENT_ID}"
                  },
                  "edge": {
                    "type": "object",
                    "required": true,
                    "description": "Edge object with all edge fields.",
                    "properties": {
                      "id": {
                        "type": "string",
                        "required": true,
                        "description": "Unique edge ID. Generate a random 24-character lowercase alphanumeric string."
                      },
                      "source": {
                        "type": "string",
                        "required": true,
                        "description": "Source node ID (the node the edge comes FROM)"
                      },
                      "target": {
                        "type": "string",
                        "required": true,
                        "description": "Target node ID (the node the edge goes TO)"
                      },
                      "type": {
                        "type": "string",
                        "fixed": "default"
                      },
                      "environmentId": {
                        "type": "string",
                        "fixed": "${ADP_ENVIRONMENT_ID}"
                      },
                      "animated": {
                        "type": "boolean",
                        "fixed": true
                      },
                      "sourceHandle": {
                        "type": "string",
                        "description": "Output handle on source node. Recommended: 'bottom' for vertical flow."
                      },
                      "targetHandle": {
                        "type": "string",
                        "description": "Input handle on target node. Recommended: 'top' for vertical flow."
                      }
                    }
                  }
                }
              }
            }
          },
          "update_edge": {
            "name": "update_edge",
            "node_type": "socketio_request",
            "description": "Update an existing edge by its ID. You can change source, target, sourceHandle, targetHandle, or type.",
            "node_schema": {
              "url": {
                "type": "string",
                "fixed": "${ADP_API_URL}"
              },
              "namespace": {
                "type": "string",
                "fixed": "/canvas"
              },
              "event": {
                "type": "string",
                "fixed": "update_edge"
              },
              "cookies": {
                "type": "string",
                "fixed": "__Secure-better-auth.session_token=${ADP_SESSION_TOKEN}"
              },
              "timeout_ms": {
                "type": "integer",
                "fixed": 15000
              },
              "payload": {
                "type": "object",
                "properties": {
                  "environmentId": {
                    "type": "string",
                    "fixed": "${ADP_ENVIRONMENT_ID}"
                  },
                  "id": {
                    "type": "string",
                    "required": true,
                    "description": "The ID of the edge to update"
                  },
                  "updates": {
                    "type": "object",
                    "required": true,
                    "description": "Fields to update on the edge (e.g., source, target, type)"
                  }
                }
              }
            }
          },
          "delete_edge": {
            "name": "delete_edge",
            "node_type": "socketio_request",
            "description": "Delete an edge from the canvas by its ID. This may cause group splits if the removed edge disconnects nodes within a group.",
            "node_schema": {
              "url": {
                "type": "string",
                "fixed": "${ADP_API_URL}"
              },
              "namespace": {
                "type": "string",
                "fixed": "/canvas"
              },
              "event": {
                "type": "string",
                "fixed": "delete_edge"
              },
              "cookies": {
                "type": "string",
                "fixed": "__Secure-better-auth.session_token=${ADP_SESSION_TOKEN}"
              },
              "timeout_ms": {
                "type": "integer",
                "fixed": 15000
              },
              "payload": {
                "type": "object",
                "properties": {
                  "environmentId": {
                    "type": "string",
                    "fixed": "${ADP_ENVIRONMENT_ID}"
                  },
                  "edgeId": {
                    "type": "string",
                    "required": true,
                    "description": "The ID of the edge to delete"
                  }
                }
              }
            }
          },
          "list_api_keys": {
            "name": "list_api_keys",
            "node_type": "http_request",
            "description": "List all API keys (provider keys) for the workspace. Returns an array of API key objects with id, name, provider, textSecret (masked), permission, workspaceId, etc. Use this to check which API keys are already available before creating LLM nodes. The 'id' field from the response is the apiKeyId you need when configuring AI nodes.",
            "node_schema": {
              "base_url": {
                "type": "string",
                "fixed": "${ADP_API_URL}"
              },
              "endpoint": {
                "type": "string",
                "fixed": "/api-keys"
              },
              "method": {
                "type": "string",
                "fixed": "GET"
              },
              "headers": {
                "type": "object",
                "fixed": {
                  "Cookie": "__Secure-better-auth.session_token=${ADP_SESSION_TOKEN}",
                  "Content-Type": "application/json"
                }
              },
              "query_params": {
                "type": "object",
                "fixed": {
                  "workspaceId": "${ADP_WORKSPACE_ID}"
                }
              }
            }
          },
          "create_api_key": {
            "name": "create_api_key",
            "node_type": "http_request",
            "description": "Create a new API key (provider key) in the workspace. IMPORTANT: Before calling this tool, you MUST ask the user for the secret key value — never guess or fabricate API keys. Use suspend/HITL to ask the user.\n\nRequired fields in the body:\n- name: Display name for the key (e.g., 'My OpenAI Key')\n- provider: One of 'openai', 'google', 'anthropic'\n- secretKey: The actual API key (ask the user for this!)\n- permission: 'WORKSPACE' or 'PROJECT'\n- workspaceId: Auto-filled\n\nOptional:\n- projectId: Required only if permission is 'PROJECT'\n\nReturns the created API key object with its 'id' which can be used as apiKeyId in AI node configs.",
            "node_schema": {
              "base_url": {
                "type": "string",
                "fixed": "${ADP_API_URL}"
              },
              "endpoint": {
                "type": "string",
                "fixed": "/api-keys"
              },
              "method": {
                "type": "string",
                "fixed": "POST"
              },
              "headers": {
                "type": "object",
                "fixed": {
                  "Cookie": "__Secure-better-auth.session_token=${ADP_SESSION_TOKEN}",
                  "Content-Type": "application/json"
                }
              },
              "body": {
                "type": "object",
                "required": true,
                "description": "API key creation payload",
                "properties": {
                  "name": {
                    "type": "string",
                    "required": true,
                    "description": "Display name for the API key (e.g., 'My Gemini Key')"
                  },
                  "provider": {
                    "type": "string",
                    "required": true,
                    "description": "Provider: 'openai', 'google', or 'anthropic'"
                  },
                  "secretKey": {
                    "type": "string",
                    "required": true,
                    "description": "The actual API key value. MUST be provided by the user — never fabricate this."
                  },
                  "permission": {
                    "type": "string",
                    "required": true,
                    "description": "'WORKSPACE' or 'PROJECT'"
                  },
                  "workspaceId": {
                    "type": "string",
                    "fixed": "${ADP_WORKSPACE_ID}"
                  },
                  "projectId": {
                    "type": "string",
                    "description": "Required only if permission is 'PROJECT'"
                  }
                }
              }
            }
          }
        }
      }
    },
    "log_result": {
      "type": "log"
    }
  },
  "edges": [
    {
      "from": "trigger",
      "to": "agent"
    },
    {
      "from": "agent.result",
      "to": "log_result"
    }
  ]
}
