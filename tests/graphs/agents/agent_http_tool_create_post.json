{
  "comment": "Run 1: agent uses HTTP tool to create a blog post via jsonplaceholder. Memory persists.",
  "nodes": {
    "trigger": {
      "type": "trigger_webhook",
      "config": {
        "path": "/create_post",
        "method": "POST",
        "test_payload": {
          "prompt": "Create a blog post about the future of AI agents."
        }
      }
    },
    "agent": {
      "type": "llm_call",
      "config": {
        "provider": "openai",
        "api_key": "${OPENAI_API_KEY}",
        "model": "gpt-4o-mini",
        "system_message": "You are a blog post writer. Use the create_blog_post tool to create posts. Be concise (2-3 sentences max for content). After the tool returns, briefly summarize the title and id you got back.",
        "connection_url": "${DATABASE_URL}",
        "enabled_tools": [
          "create_blog_post"
        ],
        "tool_configurations": {
          "create_blog_post": {
            "name": "create_blog_post",
            "node_type": "http_request",
            "description": "Create a new blog post. Author is always 'Fulanito' and posts are always published. You provide the title and content.",
            "fixed_config": {
              "base_url": "https://jsonplaceholder.typicode.com",
              "endpoint": "/posts",
              "method": "POST",
              "headers": {
                "Content-Type": "application/json",
                "X-Request-ID": "$DYNAMIC"
              },
              "body": {
                "userId": 1,
                "author": "Fulanito",
                "title": "$DYNAMIC",
                "content": "$DYNAMIC"
              }
            }
          }
        }
      }
    },
    "log": { "type": "log" }
  },
  "edges": [
    { "from": "trigger", "to": "agent" },
    { "from": "agent", "to": "log" }
  ]
}
