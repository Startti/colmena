{
  "comment": "Smoke test for inject_secrets on config. The secret stored under the handle is itself the Python source, and `show.config.code` holds only the handle (substituted by the test setup). The script runs only if inject_secrets resolved the handle in config; unresolved, Python fails on the literal handle. Stream frames carry the handle, never the source.",
  "metadata": {
    "category": "basic",
    "features": ["secure_values", "inject_in_config"],
    "requires_env": ["DATABASE_URL"]
  },
  "nodes": {
    "show": {
      "type": "python_script",
      "config": { "sandbox_mode": "none", "code": "<sv_smoke>" }
    }
  },
  "edges": []
}
